[phases.setup]
nixPkgs = ['python311', 'gcc', 'g++', 'pkg-config', 'libsndfile', 'ffmpeg', 'file']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements.txt']

[start]
cmd = 'python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}'
